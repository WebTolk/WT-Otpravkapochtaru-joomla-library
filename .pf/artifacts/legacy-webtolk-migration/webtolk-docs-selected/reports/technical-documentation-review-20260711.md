# Проверка технической документации - 2026-07-11

## Объём

- Корневой быстрый старт: `README.md`.
- Архитектурное руководство: `docs/developer-api.md`.
- Карта фасада: `docs/facade-method-reference.md`.
- 7 тематических глав в `docs/api/`.
- Справочник сущностей: `docs/entities-reference.md`.
- Низкоуровневый интерфейс: `docs/low-level-api.md`.
- Обновлённый индекс: `docs/README.md`.

Новая основная документация содержит около 2850 строк и 11 800 слов без учёта JSON-примеров и схем.

## Источники

- Реальные сигнатуры и тела публичных методов в `lib_webtolk_otpravkapochtaru/src/`.
- Сущности и их проверки/значения по умолчанию.
- Манифестная модель Joomla-библиотеки и пространство имён.
- Joomla platform contract и локальная документация `D:/.agents/docs/`.
- Реальные обезличенные ответы и наблюдаемые схемы `docs/api-schemas/otpravka/`.

## Покрытие

- `Otpravkapochtaru`: 35 публичных методов с учётом конструктора.
- Низкоуровневые классы: 29 публичных методов.
- Сущности: 17 публичных методов с учётом абстрактного `toArray()`.
- У каждого метода фасада есть блоки «Что делает», «Зачем нужен», «Как работает» и отдельный полный PHP-пример.
- Все примеры содержат нужные `use`, создание объекта и профильную обработку ошибок.

## Исправленные расхождения старой документации

- `Recipient` документирован с реальными полями `raw-address`, `raw-full-name`, `raw-telephone` и поддерживаемыми псевдонимами `address`, `name`, `phone`.
- `createBatch()` описывает фактический корневой ответ `batches` + `result-ids`.
- `getAllBatches()` и `getOrdersInBatch()` больше не объявлены вымышленными объектами `content/page/size`, если реальный ответ был списком.
- `searchPostOfficeByAddress()` описывает наблюдаемую форму `is-matched` + `postoffices`.
- Удаление и возврат заказа описывают реальные `result-ids`.
- Двоичные документы не снабжены выдуманной успешной JSON Schema: реальный прогон вернул HTTP 400.
- Для возвратов явно документировано поле `errors`, которое текущий транспорт не превращает в исключение.
- Расчёт тарифа предварён нормализацией двух адресов.
- Создание заказа предварено нормализацией адреса, ФИО и телефона.

## Обезличивание

При сверке документации исправлен локальный генератор примеров:

- добавлены поля `original-fio`, `raw-full-name` и GUID;
- регулярное выражение телефона больше не повреждает GUID;
- производные примеры и схемы сгенерированы повторно без REST-вызовов;
- 27 примеров повторно прошли структурную проверку;
- утечки частных значений аккаунта: 0.

## Автоматические проверки

Сценарий `.webtolk/tmp/verify/technical-documentation-check.php` выполнил:

- проверку 13 основных Markdown-файлов;
- сопоставление публичных методов с документацией;
- проверку 140 относительных ссылок и явных якорей;
- проверку баланса блоков кода;
- `php -l` для 60 полных PHP-примеров;
- проверку обязательных смысловых блоков каждого метода фасада.

Итог: 0 ошибок.

PHPStorm `errorsOnly` для 13 основных файлов: 0 ошибок.

## Граница поставки

- Код библиотеки не изменялся в рамках задачи документации.
- Ранее изменённый `Request.php` остаётся отдельным незакоммиченным изменением.
- Документация и `docs/api-schemas/` не входят в Joomla ZIP, поскольку `docs/` исключён конфигурацией сборки.
- Пакет пересобран общей целью Phing `3. Package release`; итоговый SHA-256: `6B5B7746D97FCECA754A4E237A4AE18B505AD9674F5D801FFD70615FAF30253F`.
- Коммит и отправка в удалённый репозиторий не выполнялись.

## Исправление разметки таблиц

- Пользовательская проверка выявила распад сигнатур с объединёнными типами, например `Recipient|array`, внутри Markdown-таблиц.
- Причина: неэкранированный `|` считается разделителем столбцов даже внутри обратных кавычек в табличной разметке.
- Исправлены 29 фрагментов объединённых типов в 8 файлах; программный код библиотеки не изменялся.
- Проверяющий сценарий дополнен контролем постоянного количества столбцов и запретом неэкранированных `|` внутри строчного кода в таблицах.
- Повторная проверка: 37 таблиц, 209 строк таблиц, 140 ссылок, 60 PHP-примеров, 0 ошибок; PHPStorm `ERROR`: 0.
