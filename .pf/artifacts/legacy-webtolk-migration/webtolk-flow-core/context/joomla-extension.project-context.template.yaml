project:
  name: plg_example
  summary: Joomla extension project with reusable development-flow overlays.
  repository_root: .
  primary_artifact_root: .webtolk

platform:
  id: joomla
  variant: extension
  extension_type: package
  platform_contract: platforms/joomla/platform.json
  platform_context_loaded: true
  platform_sources:
    required:
      - docs/joomla-toolkit/README.md
      - docs/joomla-toolkit/joomla-architecture-rules.md
    conditional:
      extension-structure:
        - docs/joomla-toolkit/joomla-extension-structures.md
      api-or-webservices:
        - docs/joomla-toolkit/joomla6-rest-api-core-full.md
  platform_checks_applied: []

repository:
  repo_root: .

deployment:
  test_joomla_instance: E:/OSPanel/home/joomla.local/public
  admin_url: http://joomla.local/administrator
  package_output: ./.webtolk/build/
  package_type: zip
  admin_credentials:
    username: codex
    password: codexcodexcodex

overlays:
  platform:
    - joomla
  toolchain:
    - php
  domain:
    - joomla-extension
  tooling:
    - phpstorm-mcp
    - serena
    - devtools
  project_extensions: []

constraints:
  forbidden_paths:
    - vendor/
    - node_modules/
    - dist/
  release_constraints:
    - Update release-notes and migration-notes before delivery.
    - Validate install, update and uninstall safety before release.
    - Preserve manifest versioning and namespace alignment.
  verification_constraints:
    - Use Joomla platform checks before implementation, assurance and release.
    - Browser verification is required for administrator UI changes.
    - Review deprecated API usage against the Joomla toolkit.

environments:
  local:
    joomla_version: "5.x or 6.x target"
    app_url: http://joomla.local
  ci:
    enabled: false
  release:
    package_format: zip
    deployment_target: Joomla extension installer

artifact_locations:
  briefs: docs/briefs
  reports: docs/reports
  release: docs/release

tooling:
  phpstorm_mcp_required: true
  serena_required: true
  devtools_required: true
  shell_fallback_allowed: true
  logical_tools:
    phpstan:
      toolchain: php
      policy: configured-toolchain
    phpunit:
      toolchain: php
      policy: configured-toolchain
    php-cs-fixer:
      toolchain: php
      policy: configured-toolchain
    phing:
      toolchain: php
      policy: configured-toolchain

joomla_expectations:
  mandatory_checks:
    - core-first-decision-making
    - service-provider-and-di-patterns
    - thin-controllers-and-model-service-boundaries
    - acl-for-state-changing-actions
    - webassetmanager-usage
    - install-update-uninstall-safety
    - deprecated-api-review
  conditional_checks:
    plugin:
      - explicit-event-contracts
      - typed-or-modern-event-patterns
    admin-ui:
      - joomla-dialog-over-bootstrap-modal
      - form-validator-webasset
    api:
      - webservices-routing-and-registration
      - api-auth-and-response-shape

notes:
  - Fill platform_context_loaded=true after loading the Joomla platform contract and toolkit sources.
  - Adjust extension_type, extension_group and repository paths for module, component, library or webservices plugin projects.
  - Keep Joomla-specific decisions in project context and overlays, not in core templates.
