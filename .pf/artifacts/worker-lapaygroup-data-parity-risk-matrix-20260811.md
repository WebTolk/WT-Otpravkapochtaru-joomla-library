# Worker Artifact: t10-lapaygroup-data-parity-risk-matrix
run_id: lapaygroup-russianpost-joomla-local-validation-20260811
task_id: t10-lapaygroup-data-parity-risk-matrix
generated_at: 2026-08-11

## Scope
Compare current Joomla library behavior to `lapaygroup/russianpost` 2.0.0 method-class contracts for migration planning.
Environment constraint: `lapaygroup/russianpost:2.0.0` was not installable here (Composer blocked by missing CLI `openssl` extension), so this matrix is based on local source contract and documented LapayGroup SDK conventions with verification gaps explicitly marked.

| current method | upstream method/class | input compatibility | output compatibility | mutation risk | data-loss risk | migration recommendation |
| --- | --- | --- | --- | --- | --- | --- |
| `Otpravkapochtaru::getAccountInfo()` / `getSettings()` | `LapayGroup\RussianPost\OtpravkaApi::settings()` | likely high: both request the account settings payload (`/1.0/settings`) and same auth model | likely medium-high: current returns raw decoded array from `Request::decodeResponse()` with business-error normalization; upstream behavior should match shape but not runtime-verified | medium: output is passed through generic array transport with error/transport exception wrapping | low: no local field filtering | add an explicit parity test: compare raw fixture of `/1.0/settings` response across both clients and preserve current normalized keys |
| `Otpravkapochtaru::getApiLimit()` | `LapayGroup\RussianPost\OtpravkaApi::settings()` (limit likely nested in account settings) | medium: may be an extra convenience method in current layer (`/1.0/settings/limit`) absent as direct upstream method | medium: extra route may differ in upstream 2.0.0 and could return a different envelope | low: minimal transformation | low: potential dropped field visibility if upstream separates `limit` object | keep `getApiLimit()` as compatibility shim in migration if upstream lacks direct equivalent |
| `Otpravkapochtaru::getShippingPoints()` | `LapayGroup\RussianPost\OtpravkaApi::shippingPoints()` | high: same endpoint intent (`/1.0/user-shipping-points`) and same input shape | medium-high: response structure expected same envelope but not executed | low | medium: downstream linked fields depend on presence of `user-available-mail-types`, `user-available-products`, `operator-postcode` | preserve exact field requirements during migration; fail fast when these fields are absent and keep safe fallback |
| `LinkedSelectOptionsService::getMailTypeOptions()` / `getMailCategoryOptions()` using `user-available-mail-types` + `user-available-products` fallback | no direct upstream helper (likely derive from `shippingPoints` data in caller) | medium: upstream may not expose this exact helper contract, so caller-side orchestration must be reimplemented | medium: currently returns `{value,text}` options, requiring label localization layer outside SDK | high: custom key selection order is business-logic-heavy and currently preserves user-facing stability | high: if fallback path changes (e.g., missing `user-available-mail-types`), option set can shrink/expand unexpectedly | create equivalent helper in project layer, and treat it as **non-SDK contract** to avoid hidden behavior regression |
| `Otpravkapochtaru::getTariff(int\|string $objectId, array $params, array $services = [])` | `LapayGroup\RussianPost\Providers\Calculation::getTariff()` (or equivalent in v2 facade) | medium: current helper forces `object` cast to string and merges services into comma-separated `service` field | medium: output likely same tariff schema from same `/1.0/tariff` endpoint | medium: client-side parameter shaping (object coercion + service join) may differ from upstream expectations | medium: unknown upstream may reject unknown service join format or preserve raw service list | keep parameter adapter in shim; add golden payload tests for `object`, `service`, and nullable query keys |
| `Otpravkapochtaru::getTariffAndDeliveryPeriod()` | `LapayGroup\RussianPost\Calculation` equivalent | medium: current calls same underlying tariff endpoint and maps delivery period from returned fields | medium: output shape may differ in key names (SDK version risk) if 2.0.0 changed DTO mapping | medium: adapter logic currently embedded in current facade | medium: risk of field rename/drop during mapping | lock current response contract in unit fixture and adapt migration layer to both full and reduced schemas |
| `createOrders()` / `editOrder()` / `findOrderById()` / `findOrderByShopId()` / `findOrderByRpo()` / `deleteOrders()` / `returnOrdersToNew()` | `LapayGroup\RussianPost\OtpravkaApi` order CRUD methods (`createOrders`, `editOrder`, `getOrder`, `findOrder`, `deleteOrder[s]`, etc.) | medium-high: signature mostly aligns; current accepts `Order|array` entity normalization and strict recipient/index fallback validation | medium: same API endpoints, but current normalizer injects defaults (`address-type-to`, `fragile`, `mail-category`, `mail-direct`, `mail-type`) and filters nulls | medium-high: entity normalization/validation can alter caller payloads before transport | medium-high: `null` fields are stripped and defaults are added; this can mask accidental missing data or change update semantics if API supports explicit nulling | keep current normalizers as adapter layer during migration and do not switch payload contract directly to upstream raw arrays |
| `createBatch()` / `getAllBatches()` / `getOrdersInBatch()` / `generateDocumentPackage()` / `generateDocumentF103()` | `LapayGroup\RussianPost\OtpravkaApi` batch/document equivalents | high: paths and parameters map to the same public names; current adds query defaults (`sort=ask`, booleans serialized as `true/false`) | medium: document endpoints return binary package in `getBinary()` (contentType/fileName/headers) which may differ from upstream raw stream handling | high: binary handling format is current-library-specific and may be normalized differently in SDK | medium: filename/contentType loss if upstream returns opaque stream object only | retain current binary wrapper for public outputs unless upstream is also proven identical |
| `searchPostOfficeByIndex()` / `searchPostOfficeByAddress()` / `searchPostOfficeByCoordinates()` / `getPostOfficeServices()` / `getPostalCodesInLocality()` | `LapayGroup\RussianPost\OtpravkaApi` postoffice methods (or equivalent provider methods) | medium-high: same endpoints and query parameters in current implementation | medium: likely same envelope; postoffice dictionaries can evolve and add fields | low | medium: query key defaults (`filter=ALL`, `top` default 3) and key naming (`top` vs API-provided aliases) may differ in 2.0.0 | keep current argument names and defaults as compatibility wrappers and diff-test against live API snapshots |
| `getOperationsByRpo()` / `getNpayInfo()` / `getTickets()` / `getOperationsByTicket()` | `LapayGroup\RussianPost\Tracking` equivalents | medium: current uses raw SOAP payloads with SoapClient and credential insertion; upstream may use same namespace methods but different request builders | medium: both return normalized arrays, but current uses `normalizeCollection()` and specific SOAP structures | high: chunking (`getTickets`) is custom (500-size chunks, `not_create` reporting) and should match behavior not assumed | medium: current returns mixed tracking structures as arrays only; upstream may use DTO object wrappers | keep SOAP chunking + normalized array contract exactly; if upstream SDK returns typed models, add adapter to preserve array-shape compatibility |
| Credentials model: `CredentialsProvider::{getAccessToken(), getAuthMode(), getUserKey(), getUserLogin(), getUserPassword(), getTrackingLogin(), getTrackingPassword()}` + `Request::headers()` + `SoapRequest` | Upstream providers (`OtpravkaApi` + `Tracking`) credentials in 2.0.0 | medium-high: both have REST token + user auth + tracking login/password; exact parameter names are the main migration risk | medium: current builds `Authorization: AccessToken <token>` and `X-User-Authorization: Basic ...`; upstream may preserve same header format but not guaranteed | high: config source may shift from Joomla plugin `Registry` to SDK construction patterns | high if plugin params differ (`user_key_or_login_and_password`, `auth_mode` variants, missing tracking creds) | add a credential adapter class that maps existing plugin params to SDK constructor/auth objects and enforces early, explicit errors when required values are absent |
